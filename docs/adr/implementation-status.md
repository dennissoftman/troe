# ADR implementation status

Audit basis: the current working tree, inspected 2026-08-27. This
ledger classifies the scope each ADR currently accepts. It does not turn a
capability that an ADR explicitly leaves for a later decision into a closure
gap. Evidence below names the primary implementation and the narrowest useful
test or gate. The broad hosted gate and all four QEMU platform suites include
the ADR 0031 TCP stream and repeated-teardown checks.

Planning note, 2026-08-26: ADR 0032's portable wait/pending-call foundation,
native timer/UDP deferred replies, isolated diagnostics server, fault cleanup,
and zero-allocation measured transport interval are implemented. General
mailboxes, persistent servers, and multiple live KEX tasks remain staged. ADR
0033 accepts the future configuration-namespace direction, and ADR 0034
consolidates the typed native-handle rule followed by existing services. Their
scoped-directory and package-activation work is explicitly deferred, so none of
these planning decisions changes the implemented closure set. ADR 0035 is a
proposed implementation contract for persistent services, protected IPC, and
the network/filesystem server migration; it is not accepted or implemented.

## Summary

| Classification | Count | ADRs |
| --- | ---: | --- |
| Implemented | 29 | 0001–0005, 0007–0008, 0010–0031 |
| Superseded + implemented | 1 | 0006 |
| Direction / deferred by explicit scope | 3 | 0009, 0033–0034 |
| Staged implementation | 1 | 0032 |
| Proposed implementation contract | 1 | 0035 |

The accepted ADR closure set has no remaining implementation gap. Physical
machines, embedded/no-MMU compositions, e1000, GPU, audio, broad filesystem
writers, and real cloud-provider drivers/import paths are explicitly later
features.

## Per-ADR ledger

| ADR | Classification | Primary evidence | Remaining gap or intentional future scope |
| --- | --- | --- | --- |
| [0001](0001-toolchain-license-and-dependencies.md) | Implemented | [`rust-toolchain.toml`](../../rust-toolchain.toml); [`scripts/audit.py`](../../scripts/audit.py); `test_exact_cargo_audit_version_is_required` in [`tests/test_audit_policy.py`](../../tests/test_audit_policy.py); `test_workspace_metadata_has_only_approved_boundary_dependencies` in [`tests/test_repository_policy.py`](../../tests/test_repository_policy.py) | None. A future dependency or MSRV change requires a new review; it is not current work. |
| [0002](0002-shell-pipelines-and-status.md) | Implemented | [`crates/troe-shell/src/lib.rs`](../../crates/troe-shell/src/lib.rs): bounded parser/pipeline tests plus streamed `<`, `>`, and `>>` execution, 16 KiB default/1 MiB selected chunk coverage, and completion; [`crates/troe-core/src/lib.rs`](../../crates/troe-core/src/lib.rs): dynamically growing 1 MiB-bounded intermediate pipeline storage | Concurrent pipeline rings, expansion, descriptor-number syntax, stderr redirection, and heredocs remain intentionally deferred. |
| [0003](0003-filesystem-formats.md) | Implemented | [`tools/mkefs.py`](../../tools/mkefs.py), [`tools/mkfat.py`](../../tools/mkfat.py), and [`docs/formats/kefs-v1.md`](../formats/kefs-v1.md); independent round-trip, truncation, extra-entry, allocation-chain, and payload checks in [`tests/test_image_builders.py`](../../tests/test_image_builders.py) | None for KEFS v1 and the fixed boot FAT image. General runtime filesystems are governed by ADR 0009. |
| [0004](0004-firmware-and-emulator-profile.md) | Implemented | [`tools/qemu-firmware-profile.json`](../../tools/qemu-firmware-profile.json); `verify_firmware` in [`scripts/qemu_profile.py`](../../scripts/qemu_profile.py); digest/size mismatch tests in [`tests/test_qemu_profile.py`](../../tests/test_qemu_profile.py) | None for the pinned regression machines. Making platform/environment selection explicit is tracked solely by ADR 0016. |
| [0005](0005-memory-ownership-direction.md) | Implemented | Paired allocation/reservation bitmaps sized from the discovered usable memory and the normalized map in [`crates/troe-memory/src/lib.rs`](../../crates/troe-memory/src/lib.rs); `reservation_skips_live_allocations_without_changing_their_state`, `frame_bitmap_matches_model_across_discontiguous_spans`, and `permanent_table_stack_and_runtime_reservations_never_enter_the_frame_pool`; owned heap and handoff mechanisms in [`crates/troe-machine/src/mechanism.rs`](../../crates/troe-machine/src/mechanism.rs) | None. |
| [0006](0006-resource-profiles.md) | Superseded + implemented | Single `ApplicationLimits::standard()` in [`crates/troe-application/src/lib.rs`](../../crates/troe-application/src/lib.rs), plus named Standard constructors in [`troe-identity`](../../crates/troe-identity/src/lib.rs), [`troe-terminal`](../../crates/troe-terminal/src/lib.rs), and [`troe-driver`](../../crates/troe-driver/src/lib.rs); `test_superseded_resource_profiles_cannot_reenter_source_apis` in [`tests/test_repository_policy.py`](../../tests/test_repository_policy.py) rejects old APIs and profile CLIs | None. The former micro/tiny/full and embedded/no-MMU branches are deliberately removed; hard ceilings and exact charging remain. |
| [0007](0007-identity-and-foreign-filesystem-mapping.md) | Implemented | Canonical IREG/IMAP/IMNT/IACL/ISEC parsing and successor validation in [`crates/troe-identity/src/lib.rs`](../../crates/troe-identity/src/lib.rs); exact layout in [`docs/formats/identity-v1.md`](../formats/identity-v1.md); host-CSPRNG provisioning in [`tools/mkidentity.py`](../../tools/mkidentity.py) and [`tests/test_identity_provisioning.py`](../../tests/test_identity_provisioning.py); generation binding in [`crates/troe-storage/src/generation.rs`](../../crates/troe-storage/src/generation.rs); ext4 raw-metadata preservation in [`crates/troe-ext4/src/lib.rs`](../../crates/troe-ext4/src/lib.rs) | None for Stage 8. A future multi-principal authorization engine and stable security-metadata VFS surface still require their own rules. |
| [0008](0008-owned-page-tables-and-wx.md) | Implemented | Mapping ownership/W^X and allocator non-overlap proofs in [`crates/troe-memory/src/lib.rs`](../../crates/troe-memory/src/lib.rs); exact x86/AArch64 leaf matrices, PE bounds, truncation, and deterministic mutation tests in [`crates/troe-machine/src/mmu.rs`](../../crates/troe-machine/src/mmu.rs); fresh-machine fault scenarios are wired in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | None in the accepted scope. Actual CR3/TTBR activation and faults remain target-gate evidence rather than host-only proofs. |
| [0009](0009-persistent-filesystems-and-partitions.md) | Direction / deferred by explicit scope | Bounded block regions in [`crates/troe-block/src/lib.rs`](../../crates/troe-block/src/lib.rs), GPT in [`crates/troe-gpt/src/lib.rs`](../../crates/troe-gpt/src/lib.rs), strict read/write FAT32/ext4 providers in [`crates/troe-fat/src/lib.rs`](../../crates/troe-fat/src/lib.rs) and [`crates/troe-ext4/src/lib.rs`](../../crates/troe-ext4/src/lib.rs), and manifest-authorized activation/reporting in [`crates/troe-storage/src/lib.rs`](../../crates/troe-storage/src/lib.rs) | FAT12/16, exFAT, NTFS, journal replay, repair, and dynamic provider loading remain future format/service decisions. |
| [0010](0010-cooperative-tasks-and-guarded-stacks.md) | Implemented | Scheduler, monotonic IDs, deterministic yield/exit, exact stack reaping, and isolation records in [`crates/troe-task/src/lib.rs`](../../crates/troe-task/src/lib.rs); immutable guarded-stack mappings in [`kernel/src/main.rs`](../../kernel/src/main.rs); guard-fault scenario in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | None. General preemption, SMP, and arbitrary suspended kernel stacks are explicitly outside this stage. |
| [0011](0011-bounded-in-process-message-dispatch.md) | Implemented | Port/handle generations, synchronous request/reply, copied messages, and owner revocation in [`crates/troe-dispatch/src/lib.rs`](../../crates/troe-dispatch/src/lib.rs); edge tests cover consumed identities after service/reply failure, generation retirement, saturated IDs/counters, hard capacities, stale endpoints, and exact owner teardown | None. Queues, deadlines, asynchronous receive, and a stable wire ABI were deliberately not claimed. |
| [0012](0012-native-text-console-and-editor-policy.md) | Implemented | Standard/injectable terminal limits, ANSI/PS2 decoding, editing, framebuffer arithmetic, rendering, invalid UTF-8 replacement, and overlong-control atomicity in [`crates/troe-terminal/src/lib.rs`](../../crates/troe-terminal/src/lib.rs); mandatory framebuffer/native-keyboard gate flags in [`scripts/test.py`](../../scripts/test.py) and assertions in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | None for the accepted console. AArch64 native keyboard awaits an explicitly bounded virtio-input capability. |
| [0013](0013-interrupt-driven-input-and-driver-resources.md) | Implemented | Bounded FIFO/drop accounting in [`crates/troe-driver/src/lib.rs`](../../crates/troe-driver/src/lib.rs); platform-owned resources/routes/priorities and owned APIC/GIC activation in [`crates/troe-platform/src/lib.rs`](../../crates/troe-platform/src/lib.rs) and [`crates/troe-machine/src/mechanism.rs`](../../crates/troe-machine/src/mechanism.rs); interrupt/delivery counters are asserted in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | None for the accepted single-CPU interrupt contract. SMP requires a new synchronization decision. |
| [0014](0014-unprivileged-task-isolation-and-teardown.md) | Implemented | Ordered terminate/revoke/reap transaction and failure retention in [`crates/troe-task/src/lib.rs`](../../crates/troe-task/src/lib.rs); atomic contiguous-frame return and global W^X alias checks in [`crates/troe-memory/src/lib.rs`](../../crates/troe-memory/src/lib.rs); native roots/call gates and contained fates in [`kernel/src/main.rs`](../../kernel/src/main.rs) and [`crates/troe-machine/src/mmu.rs`](../../crates/troe-machine/src/mmu.rs) | None for Stage 6. Preemption, SMP, shared memory, and a public application ABI are outside this ADR. |
| [0015](0015-kex-application-abi-and-execution-bounds.md) | Implemented | Parser, initial budgets, canonical startup page, loader transactions, and ABI 1.1 grow-only physical commits in [`crates/troe-application/src/lib.rs`](../../crates/troe-application/src/lib.rs), [`crates/troe-machine/src/mmu.rs`](../../crates/troe-machine/src/mmu.rs), and [`kernel/src/main.rs`](../../kernel/src/main.rs); reusable automatic growth in [`sdk/rust/troe-kex-alloc`](../../sdk/rust/troe-kex-alloc); converters and shared corpus in [`tools/troe-kex-tool`](../../tools/troe-kex-tool), [`tools/elf2kex.py`](../../tools/elf2kex.py), and [`tests/kex-corpus`](../../tests/kex-corpus) | Packaging trust, arguments, threads, dynamic linking, ASLR, arbitrary mappings, swap/overcommit, and general preemption require later decisions. |
| [0016](0016-hardware-targets-and-emulator-role.md) | Implemented | Named immutable descriptors plus bounded ACPI/FDT discovery in [`crates/troe-platform`](../../crates/troe-platform); pre-I/O validation and descriptor-dispatched virtio/ECAM in [`crates/troe-machine`](../../crates/troe-machine); deterministic FAT32/GPT bundles and accepted environment records in [`tools/mkcloud.py`](../../tools/mkcloud.py) and [`tools/cloud-environments.json`](../../tools/cloud-environments.json); complete combined-disk acceptance in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | None for Phases A and B on the two exact discoverable QEMU contracts. KVM and provider clouds remain explicitly unaccepted until their own firmware, drivers, image import, and runtime evidence exist. |
| [0017](0017-constrained-ext4-read-only-profile.md) | Implemented | Strict mount/checksum/feature/budget implementation, metadata-preserving file/directory create/replace/remove, symbolic/hard-link tests, and e2fsprogs writer interoperability in [`crates/troe-ext4/src/lib.rs`](../../crates/troe-ext4/src/lib.rs); exact e2fsprogs 1.47.4 gate and independent media verifier in [`tools/mkstorage.py`](../../tools/mkstorage.py) | Journal replay, ACL authorization/xattr exposure, external-xattr final unlink, directory removal/rename, indexed directories, and deeper extents are later profile revisions. |
| [0018](0018-volume-namespace-and-root-discovery.md) | Implemented | BMNT parsing and stable-identity resolution in [`crates/troe-mount/src/lib.rs`](../../crates/troe-mount/src/lib.rs); strict TOML source compilation in [`tools/mkstorage.py`](../../tools/mkstorage.py) and [`config/volumes.toml`](../../config/volumes.toml); external `EFI/BOOT/VOLUMES.BMT` loading in [`kernel/src/main.rs`](../../kernel/src/main.rs); automatic/manual provider retention plus the bounded volume-control service and [`mount.kex`](../../apps/mount); custom QEMU data-disk attachment in [`scripts/qemu_profile.py`](../../scripts/qemu_profile.py); preserved 1 GiB host-shared GPT/FAT32 media from [`tools/mkshared.py`](../../tools/mkshared.py); exact mount and restart-persistence assertions in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | Arbitrary target paths, unmount, hotplug, dynamic filesystem-provider loading, and a host-native ext4 interchange workflow remain later policy/service work. |
| [0019](0019-bounded-virtio-block-transport.md) | Implemented | Transport-independent geometry, feature, request, completion, and split-ring validation in [`crates/troe-virtio/src/lib.rs`](../../crates/troe-virtio/src/lib.rs); AArch64 MMIO and x86 PCI frontends in [`crates/troe-machine/src/virtio_mmio.rs`](../../crates/troe-machine/src/virtio_mmio.rs) and [`crates/troe-machine/src/virtio_pci.rs`](../../crates/troe-machine/src/virtio_pci.rs); native storage path in [`kernel/src/main.rs`](../../kernel/src/main.rs) | None for the synchronous one-request profile. IOMMU mapping, packed rings, multiqueue, and multiple in-flight DMA require a later ownership review. |
| [0020](0020-dual-slot-durability-transaction.md) | Implemented | Exact data/flush/commit/flush transaction, bounded recovery, and every-boundary failure tests in [`crates/troe-persist/src/lib.rs`](../../crates/troe-persist/src/lib.rs); process-reopen media decoder and generation assertions in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | None. The ADR's then-next SCFG activation use is now implemented under ADR 0021. |
| [0021](0021-immutable-content-store-and-generation-rollback.md) | Implemented | CSPK/GMAN hashing, canonical graph traversal, GC roots, and corruption tests in [`crates/troe-content/src/lib.rs`](../../crates/troe-content/src/lib.rs); SACT/config recovery in [`crates/troe-config/src/lib.rs`](../../crates/troe-config/src/lib.rs); complete generation/identity activation and named-predecessor recovery in [`crates/troe-storage/src/generation.rs`](../../crates/troe-storage/src/generation.rs); production and acceptance separation in [`kernel/src/main.rs`](../../kernel/src/main.rs) | None for Stage 8. Signatures and publisher trust remain Stage 9. Native mark-and-copy garbage collection is an authorized future implementation, not required for boot correctness. |
| [0022](0022-bounded-single-file-state-filesystem.md) | Implemented | STFS serialization/recovery and VFS create-replace/remove tests in [`crates/troe-statefs/src/lib.rs`](../../crates/troe-statefs/src/lib.rs); writable-provider boundary in [`crates/troe-vfs/src/lib.rs`](../../crates/troe-vfs/src/lib.rs); exact PRGN-selected production mount and acceptance-only mutation in [`kernel/src/main.rs`](../../kernel/src/main.rs); reopen checks in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | None. General directories, rename, ACLs, and multi-file transactions remain separate future work. |
| [0023](0023-bounded-virtio-network-profile.md) | Implemented | Bounded Ethernet/ARP/IPv4/ICMP/UDP/DHCP parsing and retained-state tests in [`crates/troe-net/src/lib.rs`](../../crates/troe-net/src/lib.rs); queue and device frontends in [`crates/troe-virtio/src/lib.rs`](../../crates/troe-virtio/src/lib.rs), [`virtio_mmio.rs`](../../crates/troe-machine/src/virtio_mmio.rs), and [`virtio_pci.rs`](../../crates/troe-machine/src/virtio_pci.rs); typed route lifecycle, publication ownership, DMA-reset failpoints, and used-index checks in [`crates/troe-machine/src/mechanism.rs`](../../crates/troe-machine/src/mechanism.rs); shell/QEMU exchange gates in [`crates/troe-shell/src/lib.rs`](../../crates/troe-shell/src/lib.rs) and [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | None for the accepted profile. ADR 0031 adds only bounded outbound TCP; TLS, DNS, IPv6, background jobs, general sockets, and non-virtio NICs remain explicit later capabilities. |
| [0024](0024-kex-command-apps-and-sdk.md) | Implemented | Allocation-free KCAP requirements in [`crates/troe-abi`](../../crates/troe-abi/src/lib.rs), canonical single-file package codec and KEX parser in [`crates/troe-application`](../../crates/troe-application/src/lib.rs), SDK and linker policy under [`sdk`](../../sdk/rust/troe-kex/src/lib.rs), canonical Rust builder in [`tools/troe-kex-tool`](../../tools/troe-kex-tool), target-selected flat `/bin` assembly in [`tools/mkefs.py`](../../tools/mkefs.py), and least-authority launch in [`kernel/src/main.rs`](../../kernel/src/main.rs) | ADR 0030 supersedes only the absent-artifact fallback. ADRs 0025–0029 add bounded datagram, filesystem, mutation, timer, diagnostics, and typed network authority. Package signatures and general jobs remain separate. |
| [0025](0025-kex-owned-datagram-service.md) | Implemented | Canonical datagram ABI and SDK client in [`crates/troe-abi`](../../crates/troe-abi/src/lib.rs) and [`sdk`](../../sdk/rust/troe-kex/src/lib.rs); exclusive binding, cooperative wait, status mapping, and drop-time unbind in [`kernel/src/main.rs`](../../kernel/src/main.rs); `udp.kex` send/cancel/zero-port teardown assertions in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | ADR 0031 separately supplies outbound TCP. DNS, TLS, IPv6, background jobs, and generic sockets remain out of scope. |
| [0026](0026-kex-read-only-filesystem-service.md) | Implemented | Canonical filesystem ABI 1.2 with symbolic-link listing/readlink and SDK client in [`crates/troe-abi`](../../crates/troe-abi/src/lib.rs) and [`sdk`](../../sdk/rust/troe-kex/src/lib.rs); bounded lexical VFS pagination in [`crates/troe-vfs`](../../crates/troe-vfs/src/lib.rs); generation-checked launch service in [`kernel/src/main.rs`](../../kernel/src/main.rs); filesystem KEX apps execute in every QEMU composition | ADR 0027 separately supplies mutation; ADR 0029 supplies typed network command services. |
| [0027](0027-kex-atomic-filesystem-mutation.md) | Implemented | 64-bit sequential offsets, configurable 4 KiB–1 MiB aggregation, and bounded directory/link ABI 2.0 plus SDK clients in [`crates/troe-abi`](../../crates/troe-abi/src/lib.rs) and [`sdk`](../../sdk/rust/troe-kex/src/lib.rs); direct KEX streams and bounded working buffers in [`kernel/src/main.rs`](../../kernel/src/main.rs); incremental namespace, FAT32, and checksummed ext4 writes with 2 GiB model and 128 MiB real-image/fsck stress coverage | Atomic replacement, rename, directory removal, seek, and broader durability transactions remain separate. |
| [0028](0028-kex-timer-and-diagnostics-services.md) | Implemented | Exact monotonic-time and canonical fixed diagnostics ABIs and SDK clients in [`crates/troe-abi`](../../crates/troe-abi/src/lib.rs) and [`sdk`](../../sdk/rust/troe-kex/src/lib.rs); deferred timer waits, copied isolated diagnostics composition, fixed caller-owned server call buffers, and the allocation-checked IPC matrix in [`kernel/src/main.rs`](../../kernel/src/main.rs); least-authority server artifacts under [`services/diagnostics`](../../services/diagnostics); `sleep.kex`, `mem.kex`, and contained server-fault fate in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | Wall time, jobs, timer registration, mutable diagnostics, persistent user servers, automatic restart, and ASID/PCID retention remain separate decisions. |
| [0029](0029-kex-typed-network-services.md) | Implemented | Exact observation, DHCP, and ICMP ABIs and SDK clients in [`crates/troe-abi`](../../crates/troe-abi/src/lib.rs) and [`sdk`](../../sdk/rust/troe-kex/src/lib.rs); capability-separated service adapters in [`kernel/src/main.rs`](../../kernel/src/main.rs); `net.kex`, `arp.kex`, `dhcp.kex`, and `ping.kex` behavior and cancellation in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | ADR 0031 separately supplies outbound TCP. DNS, TLS, IPv6, raw sockets, jobs, and general sockets remain out of scope. |
| [0030](0030-kex-only-ordinary-commands.md) | Implemented | [`crates/troe-shell/src/lib.rs`](../../crates/troe-shell/src/lib.rs) contains only `cd` and shared machine-action implementation methods; every application name resolves through [`kernel/src/main.rs`](../../kernel/src/main.rs), and both target roots contain the complete KEX app set; shell tests distinguish unavailable apps from unknown names and preserve non-shadowable intrinsics | Recovery now depends on the immutable KEX root and loader. Package trust/publication and interpreter-specific policies remain separate. |
| [0031](0031-bounded-kex-tcp-connect-service.md) | Implemented | Strict TCP frame codec and bounded connection machine in [`crates/troe-net/src/lib.rs`](../../crates/troe-net/src/lib.rs) and [`crates/troe-net/src/tcp.rs`](../../crates/troe-net/src/tcp.rs); adversarial transitions in [`crates/troe-net/tests/tcp_state_machine.rs`](../../crates/troe-net/tests/tcp_state_machine.rs); exact interface 13 protocol and SDK typestate in [`crates/troe-abi`](../../crates/troe-abi/src/lib.rs) and [`sdk`](../../sdk/rust/troe-kex/src/lib.rs); owner-scoped kernel adapter in [`kernel/src/main.rs`](../../kernel/src/main.rs); repeated `tcp.kex` host-peer exchanges in [`scripts/test-qemu.py`](../../scripts/test-qemu.py) | HTTP clients such as `wget`/`curl` may build on this byte stream later. DNS, TLS, inbound listen/accept, IPv6, background jobs, and general sockets remain separate decisions. |
| [0032](0032-bounded-wait-channels-and-asynchronous-mailboxes.md) | Staged implementation | Preallocated generation-checked waits and copied pending calls in [`crates/troe-task`](../../crates/troe-task/src/lib.rs); bounded composition-owned suspended contexts, deferred timer/UDP replies, non-spinning native idle/wakeup, and the first copied isolated diagnostics server in [`kernel/src/main.rs`](../../kernel/src/main.rs); portable race/teardown tests and both-target native cancellation/fault/reclaim gates | TCP deferral, persistent independently scheduled services, multiple live KEX tasks, and any general mailbox remain explicit later slices. |
| [0033](0033-desired-and-active-configuration-namespaces.md) | Direction / deferred by explicit scope | Existing immutable SCFG/GMAN generation configuration and the current KEFS `/etc` recovery files establish the model and migration baseline only. | Native `/config` storage and authorization, `/sys/config` generation projection, desired-versus-active tooling, and recovery namespace migration are explicitly deferred until native package activation. |
| [0034](0034-typed-capability-handles-and-unix-compatibility.md) | Direction / deferred by explicit scope | Existing generation-checked dispatch handles, typed SDK interfaces, and the separate filesystem, mutation, datagram, timer, diagnostics, network-control, and TCP services already conform to the native typed-handle rule. | Scoped package directory roots, heterogeneous waits, listeners, and any optional userspace BSD/POSIX facade remain later work with separate acceptance gates. |
| [0035](0035-persistent-isolated-services-and-fast-ipc.md) | Proposed implementation contract | Current one-shot diagnostics transport, copied-message counters, blocked task/wait model, bounded device transports, and typed filesystem/network ABIs establish the measured starting point only. | ABI 1.2 IPC pages, PCID/ASID retention, persistent supervision, direct handoff, wait sets, packet/block brokers, isolated network/storage/provider servers, dependency removal, and every performance/fault gate remain unimplemented. |

## Closure conclusion

All accepted current-scope ADR contracts are implemented, including ADR 0016
Phase B, shell-launched KEX commands, and the owned application datagram,
read-only filesystem, atomic mutation, monotonic timer, and typed diagnostics
services, plus capability-separated typed network observation, DHCP, and ICMP
echo. ADR 0030 removes the privileged ordinary-command fallback, leaving only
three shell intrinsics. ADR 0031 adds only a bounded owner-scoped outbound TCP
byte stream with adversarial state-machine and repeated native teardown gates;
it does not add DNS, HTTP, TLS, inbound listening, or general sockets.
ADR 0032 is not yet counted as closed because TCP deferral, persistent services,
multiple live KEX tasks, and mailboxes remain staged. ADR 0033 settles a later namespace direction but
explicitly defers its implementation, so it creates no current closure gap.
ADR 0034 records the rule already followed by current typed services while
deferring scoped roots and compatibility work; it likewise creates no current
closure gap. ADR 0035 is intentionally excluded from the accepted closure set
until its complete implementation and verification contract passes.
