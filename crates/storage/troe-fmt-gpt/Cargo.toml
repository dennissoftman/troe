[package]
name = "troe-fmt-gpt"
description = "Strict bounded read-only GPT discovery for TROE"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
troe-block = { path = "../../device/troe-block" }
troe-checksum = { path = "../../common/troe-checksum" }

[lints]
workspace = true
