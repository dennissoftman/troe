[package]
name = "troe-fmt-bmnt"
description = "Bounded boot mount manifest and deterministic volume selection for TROE"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
troe-checksum = { path = "../../common/troe-checksum" }

[lints]
workspace = true
