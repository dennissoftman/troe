[package]
name = "troe-fs-ramfs"
description = "Quota-bound writable in-memory filesystem provider for TROE"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
troe-fs-api = { path = "../troe-fs-api" }

[lints]
workspace = true
