[package]
name = "troe-shell"
description = "Bounded parser, pipeline executor, and session intrinsics for TROE"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
troe-application = { path = "../troe-application" }
troe-completion = { path = "../troe-completion" }
troe-core = { path = "../troe-core" }
troe-fs-api = { path = "../troe-fs-api" }
troe-driver = { path = "../troe-driver" }
troe-vfs = { path = "../troe-vfs" }

[lints]
workspace = true
