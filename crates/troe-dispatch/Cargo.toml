[package]
name = "troe-dispatch"
description = "Bounded in-process request/reply dispatch for TROE services"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
troe-abi = { path = "../troe-abi" }
troe-core = { path = "../troe-core" }

[lints]
workspace = true
