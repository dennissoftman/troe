[package]
name = "kllm-shell"
description = "Bounded parser, pipeline executor, and built-ins for kllm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
kllm-core = { path = "../kllm-core" }
kllm-vfs = { path = "../kllm-vfs" }

[lints]
workspace = true

