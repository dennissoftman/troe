[package]
name = "troe-service"
description = "Boot-service records, lifecycle, restart policy, and kernel continuations for TROE"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
acceptance-probes = []

[dependencies]
troe-abi = { path = "../../common/troe-abi" }
troe-dispatch = { path = "../troe-dispatch" }
troe-task = { path = "../troe-task" }

[lints]
workspace = true
