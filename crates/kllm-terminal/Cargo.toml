[package]
name = "kllm-terminal"
description = "Configurable bounded terminal input and line editing for kllm"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
kllm-core = { path = "../kllm-core" }

[lints]
workspace = true

