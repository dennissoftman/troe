[package]
name = "troe-application"
description = "Bounded KEX executable parser and application load-plan policy"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
troe-abi = { path = "../troe-abi" }

[lints]
workspace = true
