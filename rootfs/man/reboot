NAME
    reboot - reset and restart the platform

SYNOPSIS
    reboot

DESCRIPTION
    Flushes command-visible state and requests a cold platform reset. reboot is
    a permanent, non-shadowable shell intrinsic and succeeds only when the shell
    holds the machine-control capability. KEX application ABI 1.0 cannot invoke
    it.
