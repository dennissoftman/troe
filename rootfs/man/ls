NAME
    ls - list one directory

SYNOPSIS
    ls [PATH]

DESCRIPTION
    Lists immediate children in deterministic lexical order. Directories have
    a trailing slash and symbolic links have a trailing @. PATH defaults to the
    current directory.
