This is a tiny Rust operating environment.

Try:
  help
  ls /
  cat /etc/motd
  echo hello | grep ell | hexdump
  echo persistent | write /tmp/message
  cat /tmp/message

Input lines, argument counts, pipeline stages, intermediate streams, paths,
file sizes, RAMFS bytes, and RAMFS node counts are all explicitly bounded.
